import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const converstionsSlice = createSlice({
  name: "conversations",
  initialState,
  reducers: {},
});

export const {} = converstionsSlice.actions;
export default converstionsSlice.reducer;
